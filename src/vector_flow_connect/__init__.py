"""vector-flow-connect: shared vendor-API connectors.

Shape coercion only. Storage / canonicalisation stays with the
consumer (prism's `Adapter` ABC, quant_hive's `BarSource` /
`OptionsSource` Protocols).
"""

__version__ = "0.1.0"
